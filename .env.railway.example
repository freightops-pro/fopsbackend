# =============================================================================
# Railway Environment Variables for FreightOps Backend
# =============================================================================
# Copy these variables to your Railway project's environment variables
# Replace placeholder values with your actual credentials
# =============================================================================

# Environment Configuration
ENVIRONMENT=production
DEBUG=False
LOG_LEVEL=INFO

# Database Configuration (PostgreSQL - Railway will provide this)
# Railway automatically provides DATABASE_URL, but you can also set individual components
DATABASE_URL=${RAILWAY_DATABASE_URL}
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_USER=${PGUSER}
DB_PASSWORD=${PGPASSWORD}
DB_NAME=${PGDATABASE}

# Database Pool Settings
DB_POOL_SIZE=50
DB_MAX_OVERFLOW=100
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_SSL_MODE=require

# Redis Configuration (for caching and sessions)
# Add Redis to your Railway project and Railway will provide REDIS_URL
REDIS_URL=${RAILWAY_REDIS_URL}
REDIS_MAX_CONNECTIONS=50

# Application URLs
FRONTEND_URL=https://your-frontend.railway.app
BACKEND_URL=https://your-backend.railway.app
CORS_ORIGINS=https://your-frontend.railway.app,https://yourdomain.com

# JWT & Authentication
JWT_SECRET_KEY=<generate-random-secret-256-bit>
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Supabase Integration (if using)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=<your-supabase-anon-key>
SUPABASE_SERVICE_ROLE_KEY=<your-supabase-service-role-key>

# Synctera Banking API
SYNCTERA_API_KEY=<your-synctera-api-key>
SYNCTERA_API_URL=https://api.synctera.com
SYNCTERA_WEBHOOK_SECRET=<your-synctera-webhook-secret>
SYNCTERA_ENVIRONMENT=production

# ELD Integrations
# Samsara
SAMSARA_CLIENT_ID=<your-samsara-client-id>
SAMSARA_CLIENT_SECRET=<your-samsara-client-secret>
SAMSARA_API_URL=https://api.samsara.com

# Motive (formerly KeepTruckin)
MOTIVE_API_KEY=<your-motive-api-key>
MOTIVE_API_URL=https://api.gomotive.com

# Fuel Card Integrations
# WEX
WEX_API_KEY=<your-wex-api-key>
WEX_API_URL=https://api.wexinc.com

# Comdata
COMDATA_API_KEY=<your-comdata-api-key>
COMDATA_API_URL=https://api.comdata.com

# Payment Processing
STRIPE_SECRET_KEY=<your-stripe-secret-key>
STRIPE_PUBLISHABLE_KEY=<your-stripe-publishable-key>
STRIPE_WEBHOOK_SECRET=<your-stripe-webhook-secret>

# QuickBooks Integration
QUICKBOOKS_CLIENT_ID=<your-quickbooks-client-id>
QUICKBOOKS_CLIENT_SECRET=<your-quickbooks-client-secret>
QUICKBOOKS_REDIRECT_URI=https://your-backend.railway.app/quickbooks/callback
QUICKBOOKS_ENVIRONMENT=production

# File Storage (Cloudflare R2 or S3)
AWS_ACCESS_KEY_ID=<your-aws-access-key>
AWS_SECRET_ACCESS_KEY=<your-aws-secret-key>
AWS_S3_BUCKET=freightops-documents
AWS_REGION=us-east-1
# For Cloudflare R2:
# AWS_ENDPOINT_URL=https://<account-id>.r2.cloudflarestorage.com

# Email Service (SendGrid, Mailgun, etc.)
SENDGRID_API_KEY=<your-sendgrid-api-key>
FROM_EMAIL=noreply@yourcompany.com
SUPPORT_EMAIL=support@yourcompany.com

# SMS Service (Twilio)
TWILIO_ACCOUNT_SID=<your-twilio-account-sid>
TWILIO_AUTH_TOKEN=<your-twilio-auth-token>
TWILIO_PHONE_NUMBER=<your-twilio-phone>

# Webhook Notifications
SLACK_WEBHOOK_URL=<your-slack-webhook-url>
DISCORD_WEBHOOK_URL=<your-discord-webhook-url>

# Monitoring & Analytics
SENTRY_DSN=<your-sentry-dsn>
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Feature Flags
ENABLE_BANKING_MODULE=true
ENABLE_ELD_INTEGRATION=true
ENABLE_FUEL_CARDS=true
ENABLE_QUICKBOOKS=true

# Encryption Keys (for sensitive data)
ENCRYPTION_KEY=<generate-random-32-byte-key>
SSN_ENCRYPTION_KEY=<generate-random-32-byte-key>

# Port Configuration (Railway sets PORT automatically)
PORT=${PORT}

# =============================================================================
# How to Generate Secure Keys:
# =============================================================================
# JWT_SECRET_KEY: openssl rand -hex 32
# ENCRYPTION_KEY: openssl rand -base64 32
# SSN_ENCRYPTION_KEY: openssl rand -base64 32
# =============================================================================
