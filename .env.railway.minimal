# =============================================================================
# MINIMAL Railway Environment Variables - FreightOps Backend
# =============================================================================
# These are the REQUIRED variables to get the backend running
# Add these to Railway → Backend Service → Variables Tab → Raw Editor
# =============================================================================

# === CRITICAL: DATABASE (Neon PostgreSQL) ===
# Get your Neon connection string from: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://YOUR_NEON_USER:YOUR_NEON_PASSWORD@YOUR_NEON_HOST/neondb?sslmode=require

# Database Pool Settings
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=40
DB_SSL_MODE=require

# === CRITICAL: Security Keys (MUST GENERATE UNIQUE VALUES) ===
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=REPLACE_WITH_RANDOM_64_CHAR_HEX

# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=REPLACE_WITH_RANDOM_BASE64_KEY
SSN_ENCRYPTION_KEY=REPLACE_WITH_RANDOM_BASE64_KEY

# JWT Settings
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# === CRITICAL: URLs & CORS ===
# Replace with your actual Railway URLs after deployment
FRONTEND_URL=https://YOUR_FRONTEND_SERVICE.railway.app
BACKEND_URL=https://YOUR_BACKEND_SERVICE.railway.app
CORS_ORIGINS=https://YOUR_FRONTEND_SERVICE.railway.app

# === Environment ===
ENVIRONMENT=production
DEBUG=False
LOG_LEVEL=INFO
PYTHONUNBUFFERED=1

# =============================================================================
# OPTIONAL: Add these later as needed
# =============================================================================

# Redis (Optional - for caching, can add Railway Redis service later)
# REDIS_URL=redis://default:password@host:port

# Supabase (Optional - if using for auth/storage)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-key
# SUPABASE_SERVICE_ROLE_KEY=your-key

# Email (Optional - for notifications)
# SENDGRID_API_KEY=your-key
# FROM_EMAIL=noreply@yourcompany.com

# File Storage (Optional - for document uploads)
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret
# AWS_S3_BUCKET=freightops-documents
# AWS_REGION=us-east-1

# =============================================================================
# How to Generate Keys (run in terminal):
# =============================================================================
# JWT_SECRET_KEY:     openssl rand -hex 32
# ENCRYPTION_KEY:     openssl rand -base64 32
# SSN_ENCRYPTION_KEY: openssl rand -base64 32
# =============================================================================
